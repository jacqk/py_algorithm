#!/usr/bin/env python
# encoding: utf-8


def find(lst):
    max = -float('inf')
    length = len(lst)
    for i in range(length):
        s = 0
        for j in range(i, length):
            s += lst[j]:q
